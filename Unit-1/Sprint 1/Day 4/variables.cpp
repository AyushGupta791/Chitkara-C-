#include <iostream>

using namespace std;

// define global variables here.

int main(){

    // Local variables

    cout << "hello world" <<endl;
    // << Stream insertion operator

    // int age;
    // cout <<age;  // will return any random garbage value, therefore it is important to initialize the variable at the time of creation.

    int age=18;
    cout <<age;

    // variable name can start with alphabets, underscore and $.
    // variable name can contain alphanumeric characters, underscore and $.
    // variable name can not start with keyword.
    // case sensitive.
    // It is good practice to take meaningful variable name
}